In front-end i have installed the jspdf package for converting my relevant  data into pdf .it will convert a data into pdf format
instruction to install
in terminal type npm install jspdf
after installing import jspdf in your file

In payslip.js i have install html2canvas package it is used to convert html content into canvas image 
steps to install
in terminal type npm install html2canvas
after installing a package import in your respected file
